<wlpd-button-like
	class="{{ html_classes( 'block group', id in user.meta( 'likes' ) ? 'is-active' : '' ) }}"
	data-action="like_document"
	data-user-id="{{ user.id }}"
	data-post-id="{{ id }}"
>
	<button
		data-dom="like"
		type="button"
		class="{{ html_classes( 'size-6 text-cyan-very-soft relative group' ) }}"
	>
		{{- icon( 'heart-fill', { classes:
			[ 'absolute size-full inset-0 opacity-0 group-[.is-active_&]:opacity-100' ] } )
		-}}
		{{- icon( 'heart-stroke', { classes:
			[ 'absolute size-full inset-0 opacity-100 group-[.is-active_&]:opacity-0' ] } )
		-}}
	</button>
</wlpd-button-like>
