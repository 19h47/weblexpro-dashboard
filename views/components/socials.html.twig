{#
 # Socials
 #
 # @package WordPress
 # @subpackage WebLexProDashboard
 #
 # @author Jérémy Levron <jeremylevron@19h47.fr> (https://19h47.fr)
 #}

{% if socials is iterable %}
	<ul class="{{ html_classes( 'flex gap-4', classes | default( [] ) | join( ' ' ) ) }}">
		{% for social in socials %}
			<li>
				<a
					class="{{ html_classes( 'w-6 h-6 block' ) }}"
					href="{{ social.url }}"
					target="_blank"
					rel="noopener noreferer"
					aria-label="{{ social.title }}"
				>
					{{- icon( social.slug, { classes: [ 'w-full h-full fill-current' ] } ) -}}
				</a>
			</li>
		{% endfor %}
	</ul>
{% endif %}
